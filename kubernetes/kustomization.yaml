apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Tambahkan namespace jika diperlukan
# namespace: your-namespace

# Image tag override (aktifkan jika ingin menggunakan tag tertentu)
# images:
# - name: millab-frontend
#   newTag: v1.0.0
